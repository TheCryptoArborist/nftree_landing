# NFTree Landing

Clean NFTree-only landing and storefront for `nftree.net`.

- Root page: NFTree collection landing, live shop, and game links.
- `/mint`: redirects to the NFTree shop section on the root page.
- `/api/nftree-listings`: reads active NFTree listings from TradePort.

This project is intentionally NFTree-only.
